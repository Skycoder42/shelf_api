name: CI/CD for frog_api_generator

on:
  workflow_dispatch:
  push:
    paths:
      - "packages/frog_api_generator/**"
      - ".github/workflows/frog_api_generator_ci.yaml"
      - ".github/workflows/frog_api_generator_cd.yaml"

jobs:
  ci:
    name: CI
    uses: Skycoder42/dart_test_tools/.github/workflows/dart.yml@main
    with:
      workingDirectory: packages/frog_api_generator
      buildRunner: false
      unitTestPaths: ""
      removePubspecOverrides: false

  # cd:
  #   name: CD
  #   uses: Skycoder42/dart_test_tools/.github/workflows/release.yml@main
  #   needs:
  #     - ci
  #   with:
  #     workingDirectory: packages/frog_api_generator
  #     tagPrefix: frog_api_generator-v
  #   secrets:
  #     githubToken: ${{ secrets.GH_PAT }}
